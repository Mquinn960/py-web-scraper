from services import airline_service
from services import destination_service

class FileHelper():

    @staticmethod
    def save_local_image_data(models):
        # download each URL in list and create directory structure
        return None
        #print(models)

    @staticmethod
    def save_local_csv(models):
        # convert model to CSV file
        return None
        #print(models)
    